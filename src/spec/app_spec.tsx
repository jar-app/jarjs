import { init } from "../app";
import { expect } from "chai";
describe("App", () => {
  it("does stuff!", () => {
    expect(false).to.eq(false);
  });

  it("other stuff!", () => {
    expect(false).to.eq(false);
  });
});