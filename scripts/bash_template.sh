#!/bin/bash
# set -o errexit  # exit on command errors
set -o pipefail # capture pipe command failures
set -o nounset  # exit on use of unset variables
set -o verbose   # print command before printing
